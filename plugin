#!/usr/bin/env bash

function main {
  case "${PLUGIN_TASK:-}" in
    fmt)
      drone fmt > /tmp/.drone.yml
      ;;
    lint)
      drone lint
      ;;
    *)
      echo "ERROR: unknown task setting: ${PLUGIN_TASK:-}" 1>&2
      return 1
      ;;
  esac
}

main "$@"
